{
  "name": "@ispcontrol/connector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && tsx --test src/**/*.test.ts",
    "start": "node dist/main.js",
    "start:global": "node scripts/start-global.mjs",
    "dev": "node --watch --experimental-strip-types src/main.ts"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "tsx": "^4.23.1",
    "typescript": "^5.8.3"
  }
}
